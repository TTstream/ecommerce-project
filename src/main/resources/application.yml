spring:
  application:
    name: commerceflow
  profiles:
    active: local
  data:
    redis:
      repositories:
        enabled: false

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,metrics
